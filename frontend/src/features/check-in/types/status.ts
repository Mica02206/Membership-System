export type MemberActivity = { action: "check-in" | "check-out"; station: string; occurredAt: string };
export type MemberStatus = { member: { fullName: string; memberId: string; packageName: string; contact: string; address: string; packageDays: number; startedAt: string; registeredAt?: string; pictureUrl?: string }; status: "active" | "expired"; daysLeft: number; lastActivity?: MemberActivity; activityHistory?: MemberActivity[] };
